// 変数の宣言・値の代入
let youWeight = 68;
let youHeight = 1.7;

// コンソールへの出力
console.log(youWeight);
console.log(youHeight);

// 定数の宣言・値の代入
const yourBmi = youWeight / ( youHeight * youHeight );

// コンソールへの出力
console.log(yourBmi);